# Test Credentials
# No auth in this app — public portfolio, no login required.
# Contact form: POST /api/contact (name, email, message) — no credentials needed.
# Owner inbox receiving contact emails: lokeshshakthi1@gmail.com
# Contact messages also stored in MongoDB collection: contact_messages
